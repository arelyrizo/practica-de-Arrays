let arr = ["Hola",  "te quiero."];

function printOutString() {
  // Usar el método join() para unir los elementos del array en una sola cadena
  let sentence = arr.join(' Erika ');

  

// Imprimir la cadena resultante
  console.log(sentence);
}
printOutString();
