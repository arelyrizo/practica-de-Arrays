function doubleArrayValues(arr) {
  let doubledArray = arr.map(number => number * 2);
 console.log(doubledArray);
}

const inputArray = [6, 12, 15, 20];// el resultado es 12,24,30,40
doubleArrayValues(inputArray);
